dataset:
  preprocess:
    encoding:
      type: oneHotEncoding
      column: Sex
    missing_values: mean
    scale:
      method: standard
      target: inputs
  split:
    shuffle: true
    test_size: 0.2
  type: csv
model:
  algorithm: RandomForest
  type: classification
target:
- Survived
